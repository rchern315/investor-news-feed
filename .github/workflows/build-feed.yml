name: Investor News RSS Feed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired repository notice
        run: echo "This repository is retired. The active news pipeline is rchern315/central-intelligence-dashboard."
